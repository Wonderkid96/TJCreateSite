// Shared motion easing — the site's standard curve, single-sourced for Framer
// Motion transitions. CSS uses the matching `--ease` variable (globals.css).
export const EASE: [number, number, number, number] = [0.2, 0.8, 0.2, 1];
